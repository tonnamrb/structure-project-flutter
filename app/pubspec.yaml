name: app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  dio: ^5.7.0
  shared_preferences: ^2.3.3
  flutter_dotenv: ^5.1.0
  form_field_validator: ^1.1.0
  cached_network_image: ^3.3.1
  google_fonts: ^6.2.1
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  google_sign_in: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # json_serializable/build_runner can be added later when models require codegen
  # json_serializable: ^6.9.0
  # build_runner: ^2.4.12

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - .env
